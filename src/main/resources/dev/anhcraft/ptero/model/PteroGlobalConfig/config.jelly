<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <j:if test="${h.hasPermission(app.ADMINISTER)}">
        <f:section title="${%Title}">
            <f:block>
                <f:repeatableHeteroProperty field="clientConfig" hasHeader="true"/>
            </f:block>
        </f:section>
    </j:if>
</j:jelly>
